from openpyxl import load_workbook
import pandas
import os
cwd = os.getcwd()  # Get the current working directory (cwd)
#files = os.listdir(cwd)  # Get all the files in that directory
print("Files in %r: %s" % (cwd, ''))
#df = load_workbook(filename=".test.xlsx")
#print(df)